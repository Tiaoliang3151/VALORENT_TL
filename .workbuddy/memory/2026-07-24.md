# 2026-07-24 工作日志

## 球烟图标显示优化
- 修改 `css/style.css` 中球烟标记样式：
  - **名称显示**：将 `.smoke-label` 从默认隐藏(`opacity:0`，仅悬停显示)改为始终可见(`opacity:1`)，位置从圆心下方(`top:50%; margin-top:10px`)改为圆圈上方(`bottom:100%; margin-bottom:4px`)
  - **外边框不透明**：将 `.smoke-range` 边框从 `1px dashed rgba(123,104,238,0.85)`(半透明虚线) 改为 `2px solid #7b68ee`(不透明实线，加粗到2px)
  - 悬停状态：label 改为变更背景色和边框色而非切换显示
- 更新 `index.html` CSS 版本号 `v=20260724b` → `v=20260724d`

## 球烟标签样式调整（第二轮）
- 去掉 `.smoke-label` 的 background / border / padding / border-radius，改为无底纯文字，不遮挡地图
- 字号从 11px 增大到 12px（地名 map-location-label 为 10px），font-weight: 700
- 颜色从 var(--text-bright) 改为 #ffcc00（金黄色），hover 时 #ffe44d
- 添加 text-shadow 黑色光晕（0 0 4px + 0 0 8px）保证无底色时在地图上的可读性
- 线烟 .line-smoke .smoke-label 同步修改
- CSS 版本号更新为 v=20260724e

## 莲花古城基础数据布置
- 根据参考图片与隐世修所 (haven/base.js) 结构，重写 `js/data/maps/lotus/base.js`
- A/B/C 点位置调整：A(18,48)、B(50,48)、C(82,42)
- 新增 18 个地名（重生点、部署区、大道、旋转门、瀑布等），类型涵盖 spawn/site/route/room/area
- 进攻烟 13 个：A大过点烟、A常规线烟、A大隔断烟、高低差单向烟、台阶烟、A二楼烟、AB连烟、B常规线烟、B二楼烟、BC连烟、C常规线烟、瀑布烟、C警家烟
- 防守烟 11 个：A大烟、抢A大烟、树屋烟、碎石单向烟、A包隔断烟、AB常规线烟、B大烟、C大烟、C大单向烟、C包单向烟、C包隔断烟
- 更新 `index.html` 中 lotus/base.js 版本号 `v=20260724c` → `v=20260724f` 以跳过缓存
- 数据为参考图片估算坐标，可在编辑器中继续微调

## 批量填充剩余11张地图战术数据
- 参考已有地图(haven/lotus)的数据结构，为全部剩余地图编写进攻烟/防守烟/地名数据
- 所有坐标均为基于地图布局的估算值，可在编辑器中微调
- 各地图数据概览：
  - **ascent（亚海悬城）**：A(15,62)、B(85,52)，14地名/9进攻烟/7防守烟
  - **bind（源工重镇）**：A(28,68)、B(72,55)，11地名/5进攻烟/5防守烟
  - **breeze（微风岛屿）**：A(12,55)、B(88,55)，11地名/5进攻烟/5防守烟
  - **split（霓虹町）**：A(12,62)、B(85,52)，12地名/5进攻烟/5防守烟
  - **fracture（裂变峡谷）**：A(12,52)、B(88,52)，12地名/6进攻烟/6防守烟（特殊双面进攻地图）
  - **pearl（深海明珠）**：A(15,62)、B(85,52)，11地名/6进攻烟/6防守烟
  - **icebox（森寒冬港）**：A(18,55)、B(82,48)，13地名/7进攻烟/7防守烟
  - **sunset（日落之城）**：A(18,52)、B(82,52)，9地名/5进攻烟/5防守烟
  - **abyss（幽邃地窟）**：A(15,52)、B(85,52)，9地名/5进攻烟/5防守烟
  - **corrode（盐海矿镇）**：A(18,52)、B(82,52)，9地名/5进攻烟/5防守烟
  - **summit（天枢云阙）**：A(15,52)、B(85,52)，9地名/5进攻烟/5防守烟
- 批量更新 `index.html` 中所有 base.js 版本号为 `v=20260724g`
- 所有文件通过 `node --check` 语法验证

## 地图卡片缩略图 + 站位连线优化
- **地图卡片缩略图**：`renderMapCard` 函数中优先使用 `map.splash`（splash艺术图）代替 `map.image`（俯瞰图）作为选择地图界面的缩略图
- **站位连线增强**（`.stand-line` CSS）：
  - 不透明度从 0.4 提高到 0.75
  - 线条从 1px dashed 改为 2px solid
  - 添加 `filter: drop-shadow` 红色光晕
  - `::before` 伪元素从 1px 加粗到 2px，添加 box-shadow
  - 高亮状态 opacity 提升到 1，线宽 3px，加强发光效果
- 版本号更新：CSS → v=20260724h，app.js → v=20260724d

## 技能详情图片功能增强
- **`renderDetailImageSection`** 升级：新增第三个参数 `desc`（图片上方说明文字），无图时显示占位提示而非隐藏
- **所有详情类型**（lineup/smoke/wallbang/plant）统一显示 3 个图片区块：站位图、瞄点图、效果图
- **新增数据字段**：`standDesc`、`aimDesc`、`effectDesc` — 每张图片上方的文字说明
- **CSS 新增** `.detail-image-desc` 样式：深色背景 + 左侧强调线，`.detail-image-placeholder` 改为虚线边框
- **编辑器**同步更新：新增 6 个输入字段（3 图路径 + 3 说明），编辑器通过 `data-field` 自动收集保存
- **示例数据**：haven/brimstone.js 第一条 lineup 添加了 standDesc/aimDesc/effectDesc 示例
- 版本号更新：CSS → v=20260724i，app.js → v=20260724e，editor.js → v=20260724d

## 批量精简 agent lineup 数据
- 编写 `tools/trim-lineups.js` 脚本，遍历所有 325 个 agent lineup 文件
- 按 ability 分组，每个地图/英雄/技能只保留第一条作为占位
- **141 个文件被修改**，保留 235 条占位数据，删除 3849 条多余数据
- 修复脚本 bug（多余的 `]` 导致语法错误），编写 `tools/fix-syntax.js` 修复全部 141 个文件
- 全部 325 个文件通过 `node --check` 语法验证
- index.html 中所有 agent 文件版本号统一更新为 `v=20260724e`
